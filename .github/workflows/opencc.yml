# Optional draft helper only — does not commit. zh-hant is manually maintained.
name: OpenCC Docs (draft)

on:
  workflow_dispatch:

jobs:
  opencc_convert:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: run kts (local preview only)
        run: kotlinc -script opencc.main.kts
